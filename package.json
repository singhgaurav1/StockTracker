{
  "name": "stock-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0 --port 5000",
    "deploy": "wrangler deploy --temporary",
    "types": "wrangler types",
    "test": "node --test test/calc.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
